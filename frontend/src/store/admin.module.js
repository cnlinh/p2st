//import AdminService from '../services/admin.service';

const initialState = {
}

export const admin = {
  namespaced: true,
  state: initialState,
  actions: {
    //async initData({ dispatch }) {},
  },
  mutations: {
  },
};
